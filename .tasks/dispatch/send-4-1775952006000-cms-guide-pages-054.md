You have task 054: Audit log, Convex-level role enforcement, SEO checklist wiring, and full QA closeout.

Mission context:
- Current mission: CMS Guide Pages
- Goal: 10
- Validation source of truth: plans/feature-cms-guide-pages/testplan.md (Part 6: G01–G05, Part 7: B01–B03)
- Depends on tasks 051 and 053 both being complete
- This is the QA closeout task for goal 10

Read first:
- plans/feature-cms-guide-pages/README.md (sections: Admin and Governance, Acceptance Criteria AC-01–AC-37)
- plans/feature-cms-guide-pages/testplan.md (all parts)
- plans/feature-cms-guide-pages/05-audit-log-and-roles.md
- convex/guides/mutations.ts (all mutations to add audit writes to)
- convex/admin/authz.ts (existing role pattern)

Deliver:
1. Convex schema addition: guideAuditLog table
   - Fields: action, resourceType, resourceId, fieldChanges, userId, userEmail, timestamp, metadata
   - Index: by_guideId

2. Update all mutations (createGuide, updateGuide, publishGuide, unpublishGuide, changeSlug, createRedirect) to write audit records:
   - updateGuide must diff old vs new field values and store in fieldChanges array
   - All records include userId + userEmail

3. Add requireAdminRole() helper to convex/admin/authz.ts if not already present
   - Apply to: publishGuide, unpublishGuide, changeSlug, createRedirect, deleteRedirect

4. Add getGuideAuditLog({ guideId }) query — admin-only, returns entries sorted by timestamp DESC, limit 50

5. Audit log UI tab ("Activity") in the admin edit form:
   - Timeline of entries with timestamp, user email, action label, changed fields summary
   - Shows up to 50 entries, no pagination needed

6. SEO checklist panel final wiring:
   - Verify all checklist rows reflect live form state (not last-saved state)
   - H1 (EN and NL separately), metaTitle (EN/NL with char count), metaDescription (EN/NL with char count), slug (with real-time DB uniqueness check), featured image alt, robotsIndex

7. Full QA closeout — run ALL 37 acceptance criteria (AC-01–AC-37):
   - Record pass/fail for each in plans/feature-cms-guide-pages/qa-report.md
   - Fix any failures before marking this task done
   - Run npx tsc --noEmit and confirm pass
   - Run full test suite and confirm pass
   - Run pnpm run build and confirm pass

Required proof:
- Audit log records publish event with user email (G01)
- Audit log records field update with old and new values (G02)
- publishGuide rejects at DB level with editor token (G03)
- changeSlug rejects at DB level with editor token (G04)
- SEO checklist shows all green for a fully-filled guide (G05)
- npx tsc --noEmit passes (B01)
- pnpm run build passes (B02)
- All pre-existing tests pass (B03)
- qa-report.md written with all 37 criteria marked pass

When done:
- Write proof into the task using: tmux-ide task done 054 --proof "qa-report at plans/feature-cms-guide-pages/qa-report.md; all AC-01–AC-37 pass; tsc + build + tests clean"
- This closes goal 10
