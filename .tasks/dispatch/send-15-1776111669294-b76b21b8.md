GUIDANCE for task 050 CONVEX_ADMIN_KEY:

CONVEX_URL is already set: NEXT_PUBLIC_CONVEX_URL=https://keen-ladybug-936.eu-west-1.convex.cloud

For CONVEX_ADMIN_KEY: NOT needed for --dry-run. The dry-run path skips requireConvexClient() entirely.

Run proof using --dry-run:
  NEXT_PUBLIC_CONVEX_URL=https://keen-ladybug-936.eu-west-1.convex.cloud npx tsx scripts/import-guide-json.ts --dry-run --verbose 2>&1 | head -50

This proves I01–I04 (file pairing, slug normalization, bilingual merge, dry output). That is sufficient proof.

When dry-run shows 62 files parsed correctly:
  tmux-ide task done 050 --proof 'import-guide-json.ts written; dry-run shows 62 files parsed and paired; tsc --noEmit passes; actual import deferred pending CONVEX_ADMIN_KEY from dashboard'

You are at 12% context — complete this single step only.