You have task 049: Public route integration — DB-first rendering, SEO metadata, JSON-LD schemas, sitemap, and redirect middleware.

Mission context:
- Current mission: CMS Guide Pages
- Goal: 10
- Validation source of truth: plans/feature-cms-guide-pages/testplan.md (Part 2: R01–R09)
- Depends on task 048 being complete (guidePages schema and queries must exist)

Read first:
- plans/feature-cms-guide-pages/README.md (sections: Technical Architecture, SEO Requirements)
- plans/feature-cms-guide-pages/02-public-route-integration.md
- src/app/(public)/guides/[slug]/page.tsx (current template)
- src/lib/guides/content.ts (existing TypeScript content layer)
- src/app/sitemap.ts (or wherever the XML sitemap is generated)
- src/proxy.ts or src/middleware.ts (for redirect middleware)

Deliver:
- src/lib/guides/content.ts — updated with DB-first lookup via Convex, TypeScript fallback for non-DB slugs
- src/app/(public)/guides/[slug]/page.tsx — generateMetadata reads DB SEO fields; Article + FAQPage + BreadcrumbList JSON-LD uses DB content; hero OG image from DB
- src/app/sitemap.ts — includes all published guides from Convex listGuides query
- src/proxy.ts (or middleware.ts) — checks redirects table and serves 301/302 for matched paths
- Any new Convex server-side fetch utilities needed for server components

Key constraints:
- DB-first fallback must be transparent: if a guide is not in DB, the TypeScript content must render identically to before
- Do NOT delete or modify src/lib/guides/backlog.ts or guide-content.ts — they are the fallback
- Redirect middleware must not add latency to non-redirect paths — use a cached lookup with 60s TTL
- generateMetadata must not break for guides still served from TypeScript (graceful null handling)
- OG image field: use dbGuide?.heroImagePublicPath if available

Required proof:
- DB-sourced guide renders with DB metaTitle (R01)
- TypeScript fallback renders correctly for unseeded slug (R02)
- curl shows correct <title> and meta description in HTML (R03)
- Article, FAQPage, BreadcrumbList JSON-LD present in curl output (R04–R06)
- Sitemap includes guide paths (R07)
- 301 redirect works after changeSlug (R08)
- noindex emitted for robotsIndex:false guide (R09)
- npx tsc --noEmit passes (B01)

When done:
- Write proof into the task using: tmux-ide task done 049 --proof "<summary>"