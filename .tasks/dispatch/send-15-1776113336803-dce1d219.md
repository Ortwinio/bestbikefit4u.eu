URGENT — you are at 22% context. Build is still running.

When the build finishes:
- If it PASSES: immediately run: tmux-ide task claim 051 && tmux-ide task done 051 --proof 'guide template redesign complete; components: GuideBodyMarkdown, GuideSoftToolCta, GuideMidPageCta, GuideFaqAccordion; pnpm build passes'
- If it FAILS with type errors: fix only the critical ones, re-run tsc --noEmit (faster), then mark done

Do NOT wait for another full build cycle if you run out of context. Priority: get the proof recorded before context expires.