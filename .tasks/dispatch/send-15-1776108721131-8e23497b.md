You have task 048: Convex schema (guidePages, guideRevisions, redirects), mutations, queries, and seed migration.

Mission context:
- Current mission: CMS Guide Pages
- Goal: 10 — database-backed content management, batch JSON import, and conversion-optimised page template
- Validation source of truth: plans/feature-cms-guide-pages/testplan.md (Part 1: S01–S09)
- Do not use tmux-ide validate-add commands in this repo; record proof in repo artifacts and task proof notes.

Read first:
- plans/feature-cms-guide-pages/README.md (sections: Content Model, Technical Architecture)
- plans/feature-cms-guide-pages/01-convex-schema-and-migration.md
- convex/schema.ts
- convex/lib/authz.ts
- src/lib/guides/backlog.ts (understand the existing TypeScript guide structure before seeding)

Deliver:
- convex/schema.ts — updated with guidePages, guideRevisions, redirects tables and all indexes
- convex/guides/mutations.ts — createGuide, updateGuide, publishGuide, unpublishGuide, changeSlug
- convex/guides/queries.ts — getPublishedGuide, getDraftGuide, listGuides, getGuideBySlug, listRedirects
- scripts/seed-guides.ts — imports existing TypeScript guide content as published records (dry-run safe)
- scripts/lib/locale-strip.ts — stripLocalePrefix, stripGuidePrefix utilities

Key constraints:
- All mutations use v. validators for every arg — no unvalidated inputs
- requireUserId() for all mutations; publishGuide/unpublishGuide/changeSlug require admin role
- changeSlug on a published guide auto-creates a redirect record
- libraryBody field: v.optional(v.object({ en: v.string(), nl: v.string() }))
- Bilingual fields pattern: v.object({ en: v.string(), nl: v.string() })
- Seed script must be idempotent (safe to re-run — checks for existing slug before inserting)

Required proof:
- npx tsc --noEmit passes (S01)
- Schema indexes present (S02)
- Auth rejection test described and confirmed (S03, S04)
- version increment confirmed with test call (S05)
- getPublishedGuide returns null for draft, non-null after publish (S06, S07)
- changeSlug creates redirect record (S08)
- seed-guides.ts --dry-run runs without errors (S09)

When done:
- Write proof into the task using: tmux-ide task done 048 --proof "<summary>"
- Task 049, 050, and 052 activate once this task is complete