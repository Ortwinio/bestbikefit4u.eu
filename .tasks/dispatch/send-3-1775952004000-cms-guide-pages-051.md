You have task 051: Guide page template redesign — Markdown renderer, hero image, quick-answer extraction, FAQ accordion, and three conversion zones.

Mission context:
- Current mission: CMS Guide Pages
- Goal: 10
- Validation source of truth: plans/feature-cms-guide-pages/testplan.md (Part 4: T01–T16, Part 7: B01–B05)
- Depends on tasks 049 (public routing with DB) and 050 (import complete so there is real data)

Read first:
- plans/feature-cms-guide-pages/README.md (sections: Standard Guide Page Template, Visitor-to-Account Conversion Strategy)
- plans/feature-cms-guide-pages/07-guide-page-template-redesign.md
- src/app/(public)/guides/[slug]/page.tsx (current template to rewrite)
- docs/cms-import/en/003--guides--bike-fitting-for-knee-pain.json (understand libraryBody Markdown structure)
- src/components/public/ (existing public components to reuse)
- src/components/analytics/TrackedCtaLink.tsx (analytics pattern)

Install before implementing:
```bash
pnpm add react-markdown remark-gfm
```

Deliver:
- src/components/content/GuideBodyMarkdown.tsx — react-markdown renderer with Prototyper UI styles
- src/lib/guides/markdown-utils.ts — extractQuickAnswer(md) and extractFaqs(md) pure functions
- src/lib/guides/markdown-utils.test.ts — unit tests for both functions (testplan B04, B05)
- src/lib/guides/cta-resolver.ts — resolveSoftCtaTool(cluster, slug, locale) and resolveClosingCtaCopy(funnel, cluster, locale)
- src/components/content/GuideSoftToolCta.tsx — Zone A: subtle inline tool suggestion card
- src/components/content/GuideMidPageCta.tsx — Zone B: mid-page conversion CTA with value proposition
- src/components/content/GuideFaqAccordion.tsx — collapsed FAQ accordion
- src/app/(public)/guides/[slug]/page.tsx — rewritten template using all new components
- src/app/api/preview/route.ts — Next.js draft mode preview API route
- src/app/api/preview-exit/route.ts — exit draft mode

Template zone order (strict):
1. Hero image (from heroImagePublicPath)
2. PublicHero (H1 + pageBrief + chips)
3. Quick Answer 3-card block (extracted from libraryBody)
4. CTA Zone A (GuideSoftToolCta — only if tool match found)
5. Markdown body (GuideBodyMarkdown)
6. CTA Zone B (GuideMidPageCta)
7. FAQ accordion (GuideFaqAccordion — extracted from libraryBody)
8. Related guides (RelatedLinksSection with internalLinkTargets)
9. CTA Zone C (PublicCtaBand — "Start Free Fit" → /login?from=guide&slug={slug})

CTA Zone A tool matching (from README):
- saddle-height-guide → /calculators/saddle-height
- saddle-width-* → /calculators/saddle-width
- reach-and-stem-guide → /calculators/bike-fit
- crank-length-guide → /calculators/bike-fit
- cluster Pain & Discomfort → /calculators/bike-fit
- cluster Ride Types → /calculators/bike-fit
- everything else → null (Zone A not rendered)

Fallback requirement: guides NOT in Convex DB must still render correctly using TypeScript content (buildLeafSections, buildFaqs, buildQuickAnswer). Do not remove any existing fallback code paths.

Required proof:
- Hero image visible on knee pain guide (T01)
- Quick Answer cards render with extracted content (T02)
- Markdown body renders table, h2, h3, bold, lists, internal links (T03)
- Internal Markdown links use Next.js client navigation (T04)
- FAQ accordion renders collapsed items, expands on click (T05)
- Related guides section visible (T06)
- Zone A visible on saddle height guide, absent on hub (T07, T08)
- Zone A absent on nutrition guide (T09)
- Zone B copy matches funnel level (T10)
- Zone C href contains ?from=guide param (T11)
- All three zone analytics events fire (T12–T14)
- NL guide renders in Dutch (T15)
- TypeScript-only guide still renders (T16)
- extractQuickAnswer and extractFaqs unit tests pass (B04, B05)
- npx tsc --noEmit passes (B01)
- pnpm run build succeeds (B02)
- Existing test suite passes (B03)

When done:
- Write proof into the task using: tmux-ide task done 051 --proof "<summary>"
- Task 054 activates once both 051 and 053 are complete
