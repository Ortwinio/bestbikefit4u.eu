New hard gate for auth/pricing work: touched website/auth surfaces must converge on approved Prototyper UI patterns only. Do not introduce or preserve alternate UI building blocks on /login or /pricing if they are in your touched path. Done criteria now include: remove dead code in touched files, keep analytics intact, and call out any security-sensitive auth/render issue you encounter.