{
  "_version": 1,
  "id": 14,
  "title": "PDF route regression: restore expected status behavior for report PDF requests after unexpected 403 failures",
  "status": "done",
  "priority": 1,
  "assigned": "reporting-owner",
  "depends_on": ["Validation scope cleanup"],
  "files": [
    "src/app/api/reports/[sessionId]/pdf/route.ts",
    "src/app/api/reports/[sessionId]/pdf/route.test.ts"
  ],
  "acceptance": [
    "PDF route tests stop returning unexpected 403 statuses",
    "Expected 200, 404, 409, and 429 route responses are restored",
    "Related report authorization logic remains consistent with production behavior"
  ],
  "notes": [
    "PDF route behavior was restored and then extended for the temporary consumer campaign in src/app/api/reports/[sessionId]/pdf/route.ts.",
    "Confirmed on 2026-04-09: src/app/api/reports/[sessionId]/pdf/route.test.ts passes, including expected 200, 404, 409, and 429 responses."
  ]
}
